{
  "name": "calo-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vue-tsc --noEmit && vite build",
    "build:package": "vue-tsc --noEmit && VITE_BASE=/ui/ CALO_UI_TARGET=ui vite build && node scripts/copy-build.mjs",
    "build:preview": "vue-tsc --noEmit && VITE_BASE=/ui-next/ CALO_UI_TARGET=ui_next vite build && node scripts/copy-build.mjs",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "6.0.7",
    "vue": "3.5.39"
  },
  "devDependencies": {
    "@vue/test-utils": "2.4.11",
    "jsdom": "29.1.1",
    "typescript": "6.0.3",
    "vite": "8.1.0",
    "vitest": "4.1.9",
    "vue-tsc": "3.1.8"
  }
}
